machine:
  nodeLabels:
    hypervisor: TrueNAS
    networkZone: DMZ
    role: Control-Plane
  network:
    hostname: talos-dmz-cp-03
    interfaces:
      - interface: ens3
        dhcp: true
  install:
    disk: /dev/vda
cluster:
  network:
    serviceSubnets:
      - 10.96.0.0/16