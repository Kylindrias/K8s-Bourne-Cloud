machine:
  nodeLabels:
    hypervisor: XCP-NG-01
    networkZone: LAN
    role: Worker
  network:
    hostname: talos-worker-01
    interfaces:
      - interface: enx0
        dhcp: true
  install:
    disk: /dev/xvda
  disks:
    - device: /dev/xvdb
      partitions:
        - mountpoint: /var/lib/longhorn/disks/xvdb
cluster:
  network:
    serviceSubnets:
      - 10.96.0.0/16