machine:
  nodeLabels:
    hypervisor: TrueNAS
    networkZone: LAN
    role: Worker
  network:
    hostname: talos-worker-03
    interfaces:
      - interface: ens3
        dhcp: true
  install:
    disk: /dev/vda
  disks:
    - device: /dev/vdb
      partitions:
        - mountpoint: /var/lib/longhorn/disks/vdb
cluster:
  network:
    serviceSubnets:
      - 10.96.0.0/16