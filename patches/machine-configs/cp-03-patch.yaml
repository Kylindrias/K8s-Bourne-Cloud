machine:
  nodeLabels:
    hypervisor: TrueNAS
    networkZone: LAN
    role: Control-Plane
  network:
    hostname: talos-worker-03
    interfaces:
      - interface: ens3
        dhcp: true
  install:
    disk: /dev/vda
cluster:
  network:
    serviceSubnets:
      - 10.96.0.0/16