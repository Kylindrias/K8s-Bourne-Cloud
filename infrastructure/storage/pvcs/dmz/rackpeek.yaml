apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rackpeek-config
  namespace: rackpeek
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi