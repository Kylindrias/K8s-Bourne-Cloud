apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ntfy-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi # adjust storage if needed
  resources:
    requests:
      storage: 10Gi