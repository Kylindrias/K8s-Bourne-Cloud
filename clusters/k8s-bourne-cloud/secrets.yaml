apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: secrets-lan
  namespace: flux-system
spec:
  interval: 5m
  path: ../../secrets/lan
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: flux-sops-age
  dependsOn:
    - name: namespaces-shared
    - name: namespaces-lan